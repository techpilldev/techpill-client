import React from 'react'

import { useStyles } from './template.styles.js'

const Component = () => {
  const classes = useStyles()

  return(
    <div className={classes.root} > 

    </div>
  )
}

export default Component